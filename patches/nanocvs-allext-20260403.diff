--- util.c	2026-04-03 16:46:26.946928691 +0200
+++ util.c	2026-04-03 16:47:21.456766124 +0200
@@ -201,22 +201,10 @@
     sha256_hex((const uint8_t *)text, len, out_hex);
 }
 
-static int has_suffix(const char *s, const char *suffix) {
-    size_t ls = strlen(s), lf = strlen(suffix);
-    if (lf > ls) return 0;
-    return strcmp(s + ls - lf, suffix) == 0;
-}
-
+/* allext patch: accept any file; read_file_text rejects binaries (null bytes) */
 int path_is_allowed_text(const char *path) {
-    static const char *exts[] = {
-        ".php", ".go", ".md", ".txt", ".html", ".css",
-        ".js", ".json", ".sh", ".conf", ".yml", ".yaml"
-    };
-    size_t i;
-    for (i = 0; i < sizeof(exts)/sizeof(exts[0]); ++i) {
-        if (has_suffix(path, exts[i])) return 1;
-    }
-    return 0;
+    (void)path;
+    return 1;
 }
 
 int path_should_ignore(const char *relpath, const char *name) {
